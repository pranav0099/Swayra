// Removed. StudyDesk is offline-only: there is no account, no server and no
// sync. Everything you record is saved on this device with SharedPreferences
// (see `storageKey` in models.dart) and is never sent anywhere.
//
// The Express + SQLite server under `server/` is left in the project but is no
// longer contacted by the app.
